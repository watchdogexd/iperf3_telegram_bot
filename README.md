# iperf3 Telegram Bot

A Telegram bot that runs `iperf3` on a server and returns test results via Telegram.

## Requirements
- Python 3.9+
- iperf3
- Telegram Bot Token

## Usage
1. Install dependencies 
```
pip install python-telegram-bot
```
2. Configure the bot token
3. Run `bot.py`